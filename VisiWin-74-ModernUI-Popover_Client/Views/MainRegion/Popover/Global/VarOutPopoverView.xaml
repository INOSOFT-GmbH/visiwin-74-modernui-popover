<vw:View x:Class="HMI.Views.MainRegion.Popover.VarOutPopoverView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vw="http://inosoft.com/visiwin7"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=vw:VariableBaseControl, IsDesignTimeCreatable=False}"
    Height="275" Width="375"
    Background="{DynamicResource ApplicationBackgroundBrush}">

    <Grid Style="{DynamicResource PopoverRootGrid}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{DynamicResource PopoverHeaderBand}">
            <StackPanel>
                <TextBlock Text="{vw:LocalizedTextBinding LocalizableText=@Views.Popover.VarOut.PopoverTitle}" Style="{DynamicResource PopoverHeaderTitle}"/>
                <TextBlock Text="{vw:LocalizedTextBinding LocalizableText=@Views.Popover.VarOut.PopoverSubtitle}"
                           Style="{DynamicResource PopoverHeaderSubtitle}"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="1">
            <GroupBox Header="{vw:LocalizedTextBinding LocalizableText=@Views.Popover.VarOut.MasterDataHeader}" Style="{DynamicResource PopoverSectionGroupBox}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="{vw:LocalizedTextBinding LocalizableText=@Views.Popover.VarOut.VariableLabel}" Style="{DynamicResource PopoverMetaLabel}"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding VariableName}" Style="{DynamicResource PopoverMetaValue}"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="{vw:LocalizedTextBinding LocalizableText=@Views.Popover.VarOut.DescriptionLabel}" Style="{DynamicResource PopoverMetaLabel}" Margin="0,6,0,0"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding VariableText}" Style="{DynamicResource PopoverMetaValue}" Margin="0,6,0,0"/>
                </Grid>
            </GroupBox>

            <GroupBox Header="{vw:LocalizedTextBinding LocalizableText=@Views.Popover.VarOut.LimitsHeader}" Style="{DynamicResource PopoverSectionGroupBox}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="{vw:LocalizedTextBinding LocalizableText=@Views.Popover.VarOut.MinimumLabel}" Style="{DynamicResource PopoverMetaLabel}"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Variable.MinValue}" Style="{DynamicResource PopoverMetaValue}"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="{vw:LocalizedTextBinding LocalizableText=@Views.Popover.VarOut.MaximumLabel}" Style="{DynamicResource PopoverMetaLabel}" Margin="0,6,0,0"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Variable.MaxValue}" Style="{DynamicResource PopoverMetaValue}" Margin="0,6,0,0"/>
                </Grid>
            </GroupBox>
        </StackPanel>

    </Grid>
    
</vw:View>
