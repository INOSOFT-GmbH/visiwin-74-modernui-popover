<vw:View x:Class="HMI.Views.MainRegion.Popover.CloseOnBackgroundPopoverView"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:vw="http://inosoft.com/visiwin7"
         xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
         mc:Ignorable="d"
         d:DataContext="{d:DesignInstance Type=vw:VariableBaseControl, IsDesignTimeCreatable=False}"
         Height="Auto" Width="375"
         Background="{DynamicResource ApplicationBackgroundBrush}">

    <Grid Style="{DynamicResource PopoverRootGrid}">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{DynamicResource PopoverHeaderBand}" Margin="0">

            <vw:TextBlock LocalizableText="@Views.Popover.CloseOnBackground.PopoverText"
                          Style="{DynamicResource PopoverHeaderTitle}" />

        </Border>

    </Grid>

</vw:View>